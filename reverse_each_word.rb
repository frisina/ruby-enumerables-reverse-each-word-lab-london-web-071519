def reverse_each_word(string)
  split_string = string.split("")

  array_length = array.length - 1

  string_length.downto(0).each do |index|
    reversed_string << string[index]
  end
  reversed_string
end
